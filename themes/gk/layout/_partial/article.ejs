<article id="<%= post.layout %>-<%= post.slug %>" class="note article article-type-<%= post.layout %>" itemscope itemprop="blogPost">
  <div class="article-inner">
    <%- partial('post/gallery') %>
    <% if (post.link || post.title){ %>
      <header class="article-header">
        <%- partial('post/title', {class_name: 'article-title'}) %>

        <% if(is_post() || is_archive() || is_home()) { %>

        <div class="note-meta">
        <% if (!index || post.tags.length){ %>
          
            <%- partial('post/tag') %>
          
        <% } %>

          <%- partial('post/date', {class_name: 'article-date', date_format: null}) %>
        </div>
        <% } %>
      </header>
    <% } %>
    


    <div class="article-entry note-content text" itemprop="articleBody">
      <% if (post.excerpt && index){ %>
        <%- post.excerpt %>
        <% if (theme.excerpt_link){ %>
          <p class="article-more-link">
            <a href="<%- url_for(post.path) %>#more"><%= theme.excerpt_link %></a>
          </p>
        <% } %>
      <% } else { %>
        <%- post.content %>
      <% } %>
    </div>

    
<!--     <% if(is_post()) { %>
    <div class="article-meta">

      <%- partial('post/category') %>
      <% if (post.comments && config.disqus_shortname){ %>
        <p class="comment">
          <a href="<%- post.permalink %>#disqus_thread" class="article-comment-link"><%= __('article.comments') %></a>
        </p>
      <% } %>
    </div>
    <% } %> -->
<% if (is_post()){ %>
  <div class="comment-absense"><p>Раньше здесь была форма комментариев, но теперь её нет. <a href="/no-comments">Почему</a>?</p><p>Хотите обсудить содержимое заметки? Напишите мне email или <a href="https://twitter.com/glebis">сообщение в&nbsp;твиттере</a>. Я отвечаю на все вежливые сообщения, и  готов дополнить заметки ценными дополнениями.</p></div>
<% } %>



  </div>
  <% if (!index){ %>
    <%- partial('post/nav') %>
  <% } %>
</article>


<% if (is_post()){ %>
  <%- partial('subscribe') %>
<% } %>

